import { NumberToKMBPipe } from './number-to-kmb.pipe';

describe('NumberToKMBPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToKMBPipe();
    expect(pipe).toBeTruthy();
  });
});
